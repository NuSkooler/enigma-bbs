#  The repo root sets NODE_ENV=production for the BBS runtime, which makes npm
#  skip devDependencies -- and the entire docs toolchain (astro, starlight) is a
#  devDependency. Force them in regardless of the ambient NODE_ENV.
include=dev
